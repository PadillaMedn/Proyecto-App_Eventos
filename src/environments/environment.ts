// This file can be replaced during build by using the `fileReplacements` array.
// `ng build ---prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase:
    {
      apiKey: "AIzaSyCGffz4W8fV3j21Nwah-5-QPNoAK2-X0LQ",
      authDomain: "lista-de-tareas-888dd.firebaseapp.com",
      databaseURL: "https://lista-de-tareas-888dd.firebaseio.com",
      projectId: "lista-de-tareas-888dd",
      storageBucket: "lista-de-tareas-888dd.appspot.com",
      messagingSenderId: "809061904184"
    }
  
  
};

/*
 * In development mode, for easier debugging, you can ignore zone related error
 * stack frames such as `zone.run`/`zoneDelegate.invokeTask` by importing the
 * below file. Don't forget to comment it out in production mode
 * because it will have a performance impact when errors are thrown
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
